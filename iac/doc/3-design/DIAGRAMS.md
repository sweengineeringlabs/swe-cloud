# IAC SEA Visual Architecture

## Layer Stack Diagram

```
┌─────────────────────────────────────────────────────────────────────┐
│                           USER APPLICATIONS                          │
│                    (Terraform configurations)                        │
└─────────────────────────────────────────────────────────────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │      FACADE LAYER         │
                    │  facade/compute/          │
                    │  facade/storage/          │
                    │  facade/database/         │
                    │                           │
                    │  • Provider routing       │
                    │  • Size normalization     │
                    │  • Unified interface      │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │       CORE LAYER          │
                    │  core/compute/            │
                    │  core/storage/            │
                    │  core/database/           │
                    │                           │
                    │  • Resource composition   │
                    │  • Dependency mgmt        │
                    │  • Lifecycle hooks        │
                    └─────────────┬─────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │        API LAYER          │
                    │  api/compute/schema.tf    │
                    │  api/storage/schema.tf    │
                    │  api/database/schema.tf   │
                    │                           │
                    │  • Input contracts        │
                    │  • Output contracts       │
                    │  • Validation rules       │
                    └─────────────┬─────────────┘
                                  │
              ┌───────────────────┼───────────────────┐
              │                   │                   │
    ┌─────────┴─────────┐ ┌──────┴──────┐ ┌─────────┴─────────┐
    │   SPI/AWS Layer   │ │ SPI/Azure   │ │   SPI/GCP Layer   │
    │ spi/aws/          │ │ spi/azure/  │ │ spi/gcp/          │
    │ • provider.tf     │ │ • provider  │ │ • provider.tf     │
    │ • backend.tf      │ │ • backend   │ │ • backend.tf      │
    └─────────┬─────────┘ └──────┬──────┘ └─────────┬─────────┘
              │                   │                   │
    ┌─────────┴─────────┐ ┌──────┴──────┐ ┌─────────┴─────────┐
    │  PROVIDERS/AWS    │ │PROVIDERS/   │ │  PROVIDERS/GCP    │
    │ providers/aws/    │ │  Azure      │ │ providers/gcp/    │
    │ • compute/        │ │ providers/  │ │ • compute/        │
    │ • storage/        │ │  azure/     │ │ • storage/        │
    │ • database/       │ │ • compute/  │ │ • database/       │
    └───────────────────┘ └─────────────┘ └───────────────────┘

                    ┌─────────────────────────┐
                    │     COMMON LAYER        │
                    │  common/                │
                    │  • variables.tf         │
                    │  • locals.tf            │
                    │  • tags.tf              │
                    │  • validation.tf        │
                    │                         │
                    │  Used by ALL layers     │
                    └─────────────────────────┘
```

## Data Flow Diagram

### Request Flow (Creating a Compute Instance)

```
┌───────────────────────────────────────────────────────────────┐
│ USER: module "web" {                                          │
│         source = "facade/compute"                             │
│         provider_name = "aws"                                      │
│         instance_size = "medium"                              │
│       }                                                       │
└───────┬───────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│ FACADE: facade/compute/main.tf                               │
│   • Validate provider ("aws")                                │
│   • Validate size ("medium")                                 │
│   • Route to core layer                                      │
└───────┬───────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│ CORE: core/compute/main.tf                                   │
│   • Lookup: common.compute_instance_types["aws"]["medium"]   │
│   •   → Result: "t3.medium"                                  │
│   • Apply common tags                                        │
│   • Manage dependencies (network, etc.)                      │
│   • Route to provider implementation                         │
└───────┬───────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│ SPI: spi/aws/provider.tf                                     │
│   • Configure AWS provider                                   │
│   • Set region, credentials                                  │
│   • Apply default tags                                       │
└───────┬───────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│ PROVIDER: providers/aws/compute/main.tf                      │
│   resource "aws_instance" "example" {                        │
│     ami           = var.ami                                  │
│     instance_type = "t3.medium"  ← From core                │
│     tags          = var.tags     ← From common              │
│   }                                                          │
└───────┬───────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│ API: api/compute/schema.tf                                   │
│   output "instance_id" {                                     │
│     value = aws_instance.example.id                         │
│   }                                                          │
│   output "instance_type" {                                   │
│     value = "t3.medium"                                      │
│   }                                                          │
└───────┬───────────────────────────────────────────────────────┘
        │
        ▼
┌───────────────────────────────────────────────────────────────┐
│ RESPONSE: {                                                   │
│   instance_id   = "i-0123456789abcdef"                       │
│   instance_type = "t3.medium"                                │
│   public_ip     = "54.123.456.789"                           │
│   provider_name = "aws"                                      │
│   normalized_size = "medium"                                 │
│ }                                                            │
└───────────────────────────────────────────────────────────────┘
```

## Multi-Provider Routing

```
                           ┌──────────────┐
                           │ USER CONFIG  │
                           │ provider_name =   │
                           │  "aws"       │
                           └──────┬───────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │   FACADE                  │
                    │   Switch on var.provider  │
                    └─────────────┬─────────────┘
                                  │
         ┌────────────────────────┼────────────────────────┐
         │                        │                        │
    provider_name ="aws"          provider_name ="azure"        provider_name ="gcp"
         │                        │                        │
         ▼                        ▼                        ▼
┌────────────────┐       ┌────────────────┐      ┌────────────────┐
│ CORE/AWS       │       │ CORE/Azure     │      │ CORE/GCP       │
│ instance_type: │       │ vm_size:       │      │ machine_type:  │
│  "t3.medium"   │       │  "Standard_B2s"│      │  "e2-medium"   │
└────────┬───────┘       └────────┬───────┘      └────────┬───────┘
         │                        │                        │
         ▼                        ▼                        ▼
┌────────────────┐       ┌────────────────┐      ┌────────────────┐
│ aws_instance   │       │ azurerm_linux_ │      │ google_compute │
│                │       │ virtual_machine│      │ _instance      │
└────────────────┘       └────────────────┘      └────────────────┘
```

## Size Normalization Flow

```
USER INPUT
    │
    │  instance_size = "medium"
    ▼
┌─────────────────────────────────────────────┐
│ COMMON LAYER: locals.tf                     │
│                                             │
│ compute_instance_types = {                  │
│   aws   = { medium = "t3.medium"   }        │
│   azure = { medium = "Standard_B2s"}        │
│   gcp   = { medium = "e2-medium"   }        │
│ }                                           │
└─────────────────────────────────────────────┘
    │
    │  Lookup based on provider
    ▼
┌─────────────────────────────────────────────┐
│ CORE LAYER: main.tf                         │
│                                             │
│ local.instance_type = lookup(               │
│   var.compute_instance_types[var.provider], │
│   var.instance_size,                        │
│   default                                   │
│ )                                           │
└─────────────────────────────────────────────┘
    │
    │  Provider-specific type
    ▼
┌─────────────────────────────────────────────┐
│ PROVIDER LAYER                              │
│                                             │
│ AWS:   instance_type = "t3.medium"          │
│ Azure: vm_size       = "Standard_B2s"       │
│ GCP:   machine_type  = "e2-medium"          │
└─────────────────────────────────────────────┘
```

## Dependency Chain Example

```
                    ┌─────────────────┐
                    │ facade/compute  │
                    └────────┬────────┘
                             │
                  depends_on │
                             ▼
                    ┌─────────────────┐
                    │ core/compute    │
                    └────────┬────────┘
                             │
           ┌─────────────────┼─────────────────┐
           │                 │                 │
    depends_on        depends_on        depends_on
           │                 │                 │
           ▼                 ▼                 ▼
    ┌──────────┐      ┌──────────┐     ┌──────────┐
    │ Network  │      │ Security │     │   IAM    │
    │ (VPC)    │      │  Group   │     │  Role    │
    └──────────┘      └──────────┘     └──────────┘
           │                 │                 │
           └─────────────────┴─────────────────┘
                             │
                      All resolved
                             ▼
                    ┌─────────────────┐
                    │ providers/aws/  │
                    │ compute/        │
                    │ main.tf         │
                    │ aws_instance    │
                    └─────────────────┘
```

## Testing Architecture

```
┌────────────────────────────────────────────────────────┐
│ LEVEL 1: Validation Tests                             │
│ • terraform fmt -check                                 │
│ • terraform validate                                   │
│ • tflint                                               │
│ ✅ Fast, no cloud resources                           │
└────────────────────────────────────────────────────────┘
                       │
                       ▼
┌────────────────────────────────────────────────────────┐
│ LEVEL 2: Unit Tests (Terratest)                       │
│ • Test facade modules                                  │
│ • Test core modules                                    │
│ • Test provider modules                                │
│ ⚙️  Uses "test" provider (null_resource)              │
└────────────────────────────────────────────────────────┘
                       │
                       ▼
┌────────────────────────────────────────────────────────┐
│ LEVEL 3: Integration Tests                            │
│ • Test cross-layer interactions                        │
│ • Test provider switching                              │
│ • Test dependency chains                               │
│ 🌐 Uses real cloud resources (dev environment)        │
└────────────────────────────────────────────────────────┘
                       │
                       ▼
┌────────────────────────────────────────────────────────┐
│ LEVEL 4: Contract Tests                               │
│ • Validate API schemas                                 │
│ • Ensure output format consistency                     │
│ • Check variable validation                            │
│ 📋 Schema validation only                             │
└────────────────────────────────────────────────────────┘
```

## Module Composition Pattern

```
FACADE MODULE (Public Interface)
    │
    ├─→ Imports COMMON definitions
    │   • locals.compute_instance_types
    │   • locals.common_tags
    │   • var.provider validation
    │
    ├─→ Calls CORE module
    │   • Passes normalized inputs
    │   • Receives unified outputs
    │
    └─→ Exposes unified interface
        • Instance details
        • Connection info
        • Metadata

CORE MODULE (Orchestration)
    │
    ├─→ Uses API contracts
    │   • Input validation
    │   • Output standardization
    │
    ├─→ Manages dependencies
    │   • Network resources
    │   • Security resources
    │   • IAM resources
    │
    └─→ Routes to PROVIDER
        • Dynamic provider selection
        • Provider-specific config

PROVIDER MODULE (Implementation)
    │
    ├─→ Uses SPI configuration
    │   • Provider authentication
    │   • Backend state
    │   • Default tags
    │
    └─→ Creates resources
        • Native Terraform resources
        • Provider-specific syntax
```

## Legend

```
┌─────────┐
│  Box    │  Module or Layer
└─────────┘

    │       Dependency or Data Flow
    ▼       

───────────  Layer Boundary

• Bullet    Feature or Component

✅          Success/Complete
⚠️          Warning/Incomplete
❌          Error/Missing
🌐          Network/Cloud
📋          Documentation
⚙️          Configuration
```
