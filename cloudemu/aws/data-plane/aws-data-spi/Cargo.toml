[package]
name = "aws-data-spi"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "aws data spi"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "2.0"
serde_json = "1.0"
async-trait = "0.1"
tracing = "0.1"
chrono = { version = "0.4", features = ["serde"] }
# Self-contained - no external cloudemu dependencies
