[package]
name = "oracle-control-spi"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Oracle Control SPI"

[dependencies]
async-trait = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
bytes = "1.0"
